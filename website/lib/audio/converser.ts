export const converse = async (
  lastParagraphTranscription: string
): Promise<string | undefined> => {
  // TODO Instead call the agent to get a reply, send that reply and speak it

  return lastParagraphTranscription; // TODO
};
